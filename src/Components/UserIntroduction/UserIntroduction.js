import React from "react";

class UserIntroduction extends React.Component {
  render()
  {
    return (  <div className="horizontal-row">
         <div className="userintroduction-box">
            <h4 align="left"> Instead of writing lots of low-quality applications, spend more time writing highly-tailored applications. After all, a resume or cover letter with typos or grammatical errors will get rejected immediately. Resumes that are over one page, more often than not, get rejected immediately.  </h4>
          </div>

      </div>)
  }
}

export default UserIntroduction;
